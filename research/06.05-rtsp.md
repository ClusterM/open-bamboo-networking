### 6.5. RTSP / RTSPS

On models that use RTSP for live view (X1 / P1S / P2S / H-series and related — not the A1/P1 MJPEG-on-:6000 path), Studio opens an RTSP(S) session to the printer and decodes **H.264** access units.

#### Availability

| Variant | Port | TLS | Typical URL shape (Studio → `Bambu_Create`) |
|---------|------|-----|-----------------------------------------------|
| **RTSPS** | **322** | yes | `bambu:///rtsps___<user>:<passwd>@<ip>/streaming/live/1?proto=rtsps&device=…` |
| **RTSP** | **554** | no | `bambu:///rtsp___<user>:<passwd>@<ip>/streaming/live/1?proto=rtsp&…` |

Path on the printer: **`/streaming/live/1`**. Credentials are the same LAN pair: `user=bblp`, `passwd=` [access code](06-channels.md#access-code). Query params (`device`, `net_ver`, `dev_ver`, `cli_id`, `cli_ver`) are metadata for Studio/analytics — auth is user/passwd.

Which protocol Studio picks is driven by printer capability / `MachineObject` LAN proto (`LVL_Rtsps` / `LVL_Rtsp` / `LVL_Local` for MJPEG). Camera ABI and remote TUTK URLs: [§8.11](08.11-camera.md), [§9](09-abi-bambusource.md).

#### Stream shape

- RTSP(S) signalling + RTP/AVP (or interleaved) carrying **H.264**.
- TLS leaf on RTSPS is the same device cert family (`CN=<serial>`) — optional anti-MITM check as in the [hub](06-channels.md#optional-certificate-check-anti-mitm).
- Deeper SDP/NAL notes: community [OpenBambuAPI `video.md`](https://github.com/Doridian/OpenBambuAPI/blob/master/video.md); Studio consumes samples via `libBambuSource` / `wxMediaCtrl3` FFmpeg decode on current builds.

#### Server-side observations (P2S / N7)

The camera server is **live555**: the `401` challenge carries `realm="LIVE555 Streaming Media"` and Digest auth (not Basic) is what the firmware asks for.

With `Transport: RTP/AVP/TCP;unicast;interleaved=0-1` the session uses **both** interleaved channels: video RTP on channel 0 and RTCP on channel 1, where the server emits periodic **sender reports**. A receiver is therefore expected to answer with RTCP receiver reports on channel 1 (standard live555 RTP-sink liveness).

Stream geometry must be taken from the SDP's `sprop-parameter-sets` SPS rather than assumed: this printer advertises **1920x1080 @ 30 fps** (VUI timing), while other reports on X1-class hardware show a cropped 1168x720 @ ~24.7 fps. Both crop offsets and VUI timing matter, so the SPS has to be parsed, not guessed.

**Evidence:** live RTSPS session against a P2S on the LAN (SDP dump, decoded RTCP compound packets, SPS parse cross-checked with `ffprobe`).

**TODO**: confirm whether the firmware's RTP sink actually drops a session whose receiver never sends RTCP RRs, and on what timeout — reported symptom in [issue #54](https://github.com/ClusterM/open-bamboo-networking/issues/54) is an X1C stream that goes silent after ~10 s with no RTSP-level error, which has not been reproduced on a P2S.

Hub: [§6](06-channels.md).
